# NexusAuth Authentication And Authorization Flows

说明：
- 你要求的是按认证方式拆分的 Mermaid 时序图。
- 下面 3 个 `.mmd` 文件都可以直接复制到 https://mermaid.live/ 渲染。

文件列表：
- `auth-sequence-authorization-code.mmd`
- `auth-sequence-client-credentials.mmd`
- `auth-sequence-device-code.mmd`
